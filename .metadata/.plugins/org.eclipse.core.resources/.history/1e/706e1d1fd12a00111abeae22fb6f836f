package com.stars;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.context.ConfigurableApplicationContext;

import com.stars.sbeans.PatientInfo;

@SpringBootApplication
public class ValueAnnotationProject01Application {

	public static void main(String[] args) {
	
		try(
			ConfigurableApplicationContext ctx=	SpringApplication.run(ValueAnnotationProject01Application.class, args);
				){
			
			PatientInfo pInfo=ctx.getBean("pi",PatientInfo.class)
		}catch(Exception e) {
			e.printStackTrace();
			
		}
	}

}
